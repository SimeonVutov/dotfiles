# Workspace-to-monitor bindings, managed by
# scripts/hyprland/monitors/{profile-menu,mode-menu}.sh — don't hand-edit
# current.conf, it gets overwritten.
source = ~/.config/hypr/conf/workspaces/current.conf
