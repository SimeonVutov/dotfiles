
# Managed by scripts/hyprland/monitors/{profile-menu,mode-menu}.sh (SUPER
# SHIFT M) — don't hand-edit current.conf, it gets overwritten. Old presets
# (external_monitor_only.conf, home_dual_monitors.conf, etc.) kept for
# reference, no longer sourced by default.
source = ~/.config/hypr/conf/monitors/current.conf
